#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run quality checks (fast checks only - comprehensive checks run in CI/CD)
set -e

echo "🔍 Running pre-commit checks..."

# Run linting
echo "  Running ESLint..."
npm run lint

# Run type checking
echo "  Running TypeScript type check..."
npm run type-check

echo "✅ Pre-commit checks passed!"
